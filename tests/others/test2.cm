int
fib (int n)
{
    if (n <= 2)
    {
	return 1;
    }
    else
    {
	return fib(n - 2) + fib(n - 1);
    }
}

int
main (void)
{
    int m;
    int n;
    m = n = fib(10);
    m = pow((n * 5) + 2 / (3 - 2 * 1 * 1), 2);
    if (m == n)
	/* not possible to get here, so what the hell, just blow up */
	return fib(fib(fib(10)));
    else
	return 0;
}
